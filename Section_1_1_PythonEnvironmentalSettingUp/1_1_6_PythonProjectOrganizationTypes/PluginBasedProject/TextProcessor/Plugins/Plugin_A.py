# Interfacing function
def process(text):
    """
    Converts the input text to uppercase.
    Args:
        text (str): The string to be transformed.
    Returns:
        str: The input text converted to all uppercase characters.
    """
    return text.upper();